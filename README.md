Hardware
Raspberry Pi Pico W

sudo apt install python3 python3-pip python3.13-venv

// create virtual environment to run ampy(upload files to device)
cd ~
python3 -m venv venv_ampy
source venv_ampy/bin/activate
pip install adafruit-ampy


